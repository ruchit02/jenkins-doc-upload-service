apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc
  namespace: elastic-system
spec:
  storageClassName: local-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi